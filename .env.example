# Copy to .env for local overrides. .env is gitignored.

# Django
SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Set DEBUG=False and a real SECRET_KEY + ALLOWED_HOSTS in production,
# then run: python manage.py collectstatic --noinput
